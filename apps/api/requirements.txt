fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13
ruff>=0.7
prometheus-client>=0.20.0
celery[redis]>=5.3
redis>=5.0
PyJWT>=2.8
httpx>=0.27
# or-tools is NOT listed here deliberately: its PyPI index page is blocked on some
# networks, so it installs from pinned local wheels (see /wheels in the Dockerfiles).
pandas>=2.0
numpy>=1.26
xgboost>=2.0
pytest>=8.0
